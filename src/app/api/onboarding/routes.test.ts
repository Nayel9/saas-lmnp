import { describe, it, expect } from "vitest";

describe("onboarding routes placeholder", () => {
  it("placeholder passe", () => {
    expect(true).toBe(true);
  });
});

// Tests API onboarding déplacés / couverts via actions.test (wizard). Fichier neutralisé.
